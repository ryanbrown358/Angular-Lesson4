import { SkillLevelPipe } from './skill-level.pipe';

describe('SkillLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
